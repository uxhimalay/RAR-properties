import { AdminScreen } from "@/components/admin/AdminApp";

export const dynamic = "force-dynamic";

/** The screen for the current path; the document itself is held by the layout. */
export default function AdminPage() {
  return <AdminScreen />;
}
